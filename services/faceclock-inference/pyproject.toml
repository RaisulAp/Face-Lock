[project]
name = "faceclock-inference"
version = "0.1.0"
description = "Face recognition inference microservice for Faceclock (Fase 0: stub only)"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
