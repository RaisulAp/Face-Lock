# Not yet enforced by the Fase 0 stub (GET /health and POST /v1/embed take
# no auth). Fase 2 wires FACE_ENGINE_TOKEN into an Authorization check on
# every route except /health, matching INFERENCE_TOKEN in deploy/.env.example.
FACE_ENGINE_TOKEN=
